import { Sora, Gloock } from "next/font/google";

export const sora = Sora({
  subsets: ["latin"],
  display: "swap",
});

export const gloock = Gloock({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
